import {SocialCard} from "@e280/scute"

export const socialCard = (title: string): SocialCard => ({
	title,
	themeColor: "#38ff7a",
	siteName: "vaultverse",
	description: "there is no way back, only deeper links.",
	image: `https://i.postimg.cc/qRmy5nrr/unnamed.png`,
})
